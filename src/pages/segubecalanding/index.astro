---
import Contact from '../../components/Contact.astro';
---

<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  
  <!-- Bloquear indexación -->
  <meta name="robots" content="noindex, nofollow" />
  <meta name="googlebot" content="noindex, nofollow" />

  <title>Segubeca | Asegura el futuro educativo de tus hijos | Asesores Seguros Monterrey</title>
  <meta name="description" content="Segubeca te ayuda a asegurar la educación universitaria de tus hijos. Garantiza su futuro con capital disponible a los 18 años y protección en caso de fallecimiento." />
  <meta name="keywords" content="Segubeca, seguro educativo, futuro hijos, educación universitaria, ahorro educación, financiamiento educativo" />
  <meta name="author" content="Segubeca" />

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />

  <style>
    body { font-family: 'Poppins', sans-serif; color: #1f2937; background: #f8fafc; margin: 0; padding: 0; }
    .btn-pill { border-radius: 50px; padding: 0.9rem 1.5rem; font-weight: 600; }
    .hero { 
      background: linear-gradient(180deg, #e6f2ff, #ffffff); 
      height: 100vh; 
      display: flex; 
      flex-direction: column;
      margin: 0;
      padding: 0;
      position: relative;
      overflow: hidden;
    }
    
    /* Elementos de fondo espaciales */
    .hero::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background-image: 
        url('/segubecalanding/stars.png'),
        url('/segubecalanding/moon.png'),
        url('/segubecalanding/planet.png');
      background-position: 
        center center,
        top 10% right 10%,
        bottom 20% left 15%;
      background-repeat: 
        repeat,
        no-repeat,
        no-repeat;
      background-size: 
        auto,
        clamp(80px, 15vw, 150px),
        clamp(100px, 20vw, 200px);
      opacity: 0.3;
      z-index: 1;
      pointer-events: none;
    }
    
    .hero .container {
      position: relative;
      z-index: 2;
      height: 100%;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      padding-top: 20px;
      padding-bottom: 20px;
    }
    .hero-logos {
      flex-shrink: 0;
      margin-bottom: 20px;
    }
    .hero-content {
      flex: 1;
      display: flex;
      align-items: center;
      min-height: 0;
    }
    .hero h1 { 
      font-weight: 700; 
      font-size: clamp(1.8rem, 4vw, 3.2rem);
      margin-bottom: 1rem;
      line-height: 1.2;
    }
    .hero p { 
      color: #475569; 
      font-size: clamp(1rem, 2.5vw, 1.2rem);
      margin-bottom: 1.5rem;
    }
    .hero-logos img {
      height: clamp(40px, 8vw, 80px);
      max-width: clamp(120px, 25vw, 200px);
      object-fit: contain;
    }
    .hero-image {
      max-height: 70vh;
      object-fit: contain;
      width: 100%;
    }
    .benefit { border: 1px solid #e5e7eb; border-radius: 1rem; padding: 1.5rem; background: #fff; height: 100%; }
    .cta-band { background: linear-gradient(90deg, #1e88e5, #1565c0); color: #fff; padding: 50px 0; }
    .cta-band h2 { font-size: 2rem; font-weight: 700; }
    .footer { background: #0b1324; color: #cbd5e1; padding: 40px 0; }
    .footer a { color: #93c5fd; text-decoration: none; }
    .section { padding: 60px 0; }
    
    /* Responsivo para móviles */
    @media (max-width: 768px) {
      .hero {
        height: 100vh;
        padding: 0;
      }
      
      /* Ajustar elementos espaciales en móviles */
      .hero::before {
        background-position: 
          center center,
          top 5% right 5%,
          bottom 10% left 5%;
        background-size: 
          auto,
          clamp(60px, 12vw, 100px),
          clamp(80px, 16vw, 120px);
        opacity: 0.2;
      }
      
      .hero .container {
        padding-top: 15px;
        padding-bottom: 15px;
      }
      
      .hero-logos {
        margin-bottom: 15px;
      }
      
      .hero-logos img {
        height: clamp(35px, 10vw, 60px);
        max-width: clamp(100px, 30vw, 150px);
      }
      
      .hero h1 {
        font-size: clamp(1.5rem, 6vw, 2.2rem);
        margin-bottom: 0.8rem;
      }
      
      .hero p {
        font-size: clamp(0.9rem, 3.5vw, 1.1rem);
        margin-bottom: 1.2rem;
      }
      
      .btn-pill {
        padding: 0.7rem 1.2rem;
        font-size: 0.9rem;
      }
      
      .hero-content .row {
        height: 100%;
      }
      
      /* Ajustar imagen hero para móviles */
      .hero-image {
        position: absolute !important;
        top: -15rem !important;
        right: 0 !important;
        width: clamp(300px, 70vw, 800px) !important;
        max-height: 80vh !important;
        object-fit: contain !important;
      }
    }
    
    @media (max-width: 576px) {
      .hero-logos img {
        height: clamp(30px, 12vw, 50px);
        max-width: clamp(80px, 35vw, 120px);
      }
      
      /* Elementos espaciales más sutiles en móviles pequeños */
      .hero::before {
        background-position: 
          center center,
          top 3% right 3%,
          bottom 5% left 3%;
        background-size: 
          auto,
          clamp(40px, 10vw, 70px),
          clamp(60px, 14vw, 90px);
        opacity: 0.15;
      }
      
      .d-flex.flex-wrap {
        flex-direction: column !important;
        gap: 0.5rem !important;
      }
      
      .btn-pill {
        width: 100%;
        text-align: center;
      }
      
      /* Imagen más pequeña en móviles muy pequeños */
      .hero-image {
        position: absolute !important;
        top: -12rem !important;
        right: 0 !important;
        width: clamp(250px, 65vw, 600px) !important;
        max-height: 70vh !important;
        object-fit: contain !important;
      }
      
      /* Ajustar texto para móviles pequeños */
      .hero h1 {
        font-size: clamp(1.3rem, 5.5vw, 2rem);
        line-height: 1.1;
      }
      
      .hero p {
        font-size: clamp(0.85rem, 3.2vw, 1rem);
      }
    }
    
    /* Para pantallas muy pequeñas (landscape en móviles) */
    @media (max-width: 480px) {
      .hero-image {
        position: absolute !important;
        top: -10rem !important;
        right: 0 !important;
        width: clamp(220px, 60vw, 500px) !important;
        max-height: 65vh !important;
        object-fit: contain !important;
      }
    }

    /* Estilos para la sección de universidades */
    .university-logo img:hover {
      filter: brightness(0) saturate(100%) invert(15%) sepia(99%) saturate(1500%) hue-rotate(200deg) brightness(80%) contrast(110%) !important;
      transform: scale(1.05);
    }

    .cost-amount:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 35px rgba(20, 89, 149, 0.4) !important;
    }

    @media (max-width: 768px) {
      .universities-section {
        padding: 60px 0 !important;
      }
      
      .university-logo {
        height: 100px !important;
      }
      
      .cost-amount p {
        font-size: clamp(1.3rem, 2.5vw, 1.8rem) !important;
      }
    }

    @media (max-width: 576px) {
      .universities-section {
        padding: 40px 0 !important;
      }
      
      .university-logo {
        height: 80px !important;
      }
      
      .cost-amount {
        padding: 15px !important;
      }
    }
  </style>
</head>

<body>
  <!-- HERO -->
<header class="hero" style="background: linear-gradient(90deg, #145995, #0f1f32);">
    <div class="container">
        <!-- Logos en la parte superior -->
        <div class="row justify-content-between align-items-center hero-logos">
            <a class="col-auto" href="/">
              <div>
                <img src="/segubecalanding/Logo_New_York_Life.webp" alt="New York Life" class="img-fluid" style="width:clamp(100px,28vw,280px);height:auto;max-height:none;" />
              </div>
            </a>

            <!--<a class="col-auto" href="/"><div >
                <img src="/ryp.svg" alt="Ramírez y Plascencia" class="img-fluid" />
            </div></a>-->
        </div>
        
        <!-- Contenido principal del hero -->
        <div class="hero-content">
            <div class="row align-items-center g-4 h-100">
                <div class="col-lg-6 d-flex flex-column justify-content-center">
                    <h1 style="color: #FFDE59;">🎓 Asegura el FUTURO de tus hijos con <span style="color:#1e88e5">Segubeca</span></h1>
                    <p style="color: white;">La educación universitaria es la mejor herencia que puedes darles. Con <strong>Segubeca</strong>, tendrás la tranquilidad de que, pase lo que pase, tus hijos contarán con los recursos necesarios para cumplir sus sueños.</p>
                    <div class="d-flex flex-wrap gap-2 mt-4">
                        <a href="#form" class="btn btn-pill btn-primary" style="background:#FFDE59; border-color:#1e88e5; color:black"><i class="fa-solid fa-graduation-cap me-2 text-dark"></i>Quiero asegurar su educación</a>
                    </div>
                </div>
                <div class="col-lg-6 d-flex justify-content-center position-relative" style="min-height:1px;">
                    <img src="/segubecalanding/hero.png" alt="Padres asegurando el futuro de sus hijos con Segubeca" class="img-fluid hero-image" style="position:absolute; top:-23rem; right:0; width:clamp(400px,75vw,1200px); max-height:95vh; object-fit:contain;" />
                </div>
            </div>
        </div>
    </div>
</header>




  <!-- UNIVERSIDADES SECTION -->
  <section class="universities-section" style="background: linear-gradient(135deg, #FFDE59, #FFD700); padding: 80px 0;">
    <div class="container">
      <div class="text-center mb-5">
        <h2 class="h1 mb-4" style="color: #145995; font-weight: 700; font-size: clamp(2rem, 4vw, 3rem);">
          ¿Cuánto cuesta estudiar en las mejores universidades de México?
        </h2>
        <p style="color: #0f1f32; font-size: clamp(1rem, 2.5vw, 1.2rem); max-width: 800px; margin: 0 auto;">
          Conoce el costo real de una educación de calidad y planifica el futuro de tus hijos
        </p>
      </div>
      
      <div class="row g-5 justify-content-center">
        <!-- Universidad Anáhuac -->
        <div class="col-md-4 col-lg-3">
          <div class="text-center">
            <div class="university-logo mb-3" style="height: 120px; display: flex; align-items: center; justify-content: center;">
              <img src="/segubecalanding/anahuac.png" alt="Universidad Anáhuac" style="max-height: 100%; max-width: 100%; object-fit: contain; filter: brightness(0) saturate(100%) invert(23%) sepia(99%) saturate(1234%) hue-rotate(200deg) brightness(91%) contrast(101%); transition: filter 0.3s ease;" />
            </div>
            <div class="cost-amount" style="background: #145995; color: white; padding: 20px; border-radius: 15px; box-shadow: 0 8px 25px rgba(20, 89, 149, 0.3);">
              <p style="margin: 0; font-weight: 700; font-size: clamp(1.5rem, 3vw, 2.2rem);">$2,350,000</p>
            </div>
          </div>
        </div>

        <!-- Universidad Panamericana -->
        <div class="col-md-4 col-lg-3">
          <div class="text-center">
            <div class="university-logo mb-3" style="height: 120px; display: flex; align-items: center; justify-content: center;">
              <img src="/segubecalanding/panamericana.png" alt="Universidad Panamericana" style="max-height: 100%; max-width: 100%; object-fit: contain; filter: brightness(0) saturate(100%) invert(23%) sepia(99%) saturate(1234%) hue-rotate(200deg) brightness(91%) contrast(101%); transition: filter 0.3s ease;" />
            </div>
            <div class="cost-amount" style="background: #145995; color: white; padding: 20px; border-radius: 15px; box-shadow: 0 8px 25px rgba(20, 89, 149, 0.3);">
              <p style="margin: 0; font-weight: 700; font-size: clamp(1.5rem, 3vw, 2.2rem);">$2,000,000</p>
            </div>
          </div>
        </div>

        <!-- Tecnológico de Monterrey -->
        <div class="col-md-4 col-lg-3">
          <div class="text-center">
            <div class="university-logo mb-3" style="height: 120px; display: flex; align-items: center; justify-content: center;">
              <img src="/segubecalanding/tec.png" alt="Tecnológico de Monterrey" style="max-height: 100%; max-width: 100%; object-fit: contain; filter: brightness(0) saturate(100%) invert(23%) sepia(99%) saturate(1234%) hue-rotate(200deg) brightness(91%) contrast(101%); transition: filter 0.3s ease;" />
            </div>
            <div class="cost-amount" style="background: #145995; color: white; padding: 20px; border-radius: 15px; box-shadow: 0 8px 25px rgba(20, 89, 149, 0.3);">
              <p style="margin: 0; font-weight: 700; font-size: clamp(1.5rem, 3vw, 2.2rem);">$1,500,000</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Call to Action dentro de la sección -->
      <div class="text-center mt-5">
        <div style=" padding: 30px;">
          <a href="#form" class="btn btn-pill" style="background: #145995; color: white; border: none; padding: 12px 30px; font-weight: 600;">
            <i class="fa-solid fa-shield-heart me-2"></i>Proteger su futuro ahora
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA BAND -->
  <section class="cta-band text-center">
    <div class="container">
      <h2>👨‍👩‍👧 Imagina ver a tus hijos graduarse gracias a tu decisión de HOY</h2>
      <p class="mb-4">Cada año las colegiaturas aumentan. Hoy puedes asegurar su educación con Segubeca.</p>
      <a href="#form" class="btn btn-light btn-pill"><i class="fa-solid fa-file-pen me-2"></i>Solicitar información</a>
    </div>
  </section>

  <!-- FORMULARIO -->
   <div id="form">  <Contact />
</div>

  <!-- FOOTER -->
  <footer class="footer">
    <div class="container text-center">
      <p>© <span id="year"></span> Segubeca. Todos los derechos reservados.</p>
      <p class="small">Consulta términos, condiciones, coberturas, exclusiones y requisitos con nuestros asesores. La disponibilidad y beneficios están sujetos a evaluación.</p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    const yearElement = document.getElementById('year');
    if (yearElement) {
      yearElement.textContent = new Date().getFullYear().toString();
    }
  </script>
</body>
</html>
