---
// Plans section with floating bubbles
import Bubble from './Bubble.astro';

const plans = [
  {
    title: "Seguros de Vida",
    image: "/seguro-vida.png",
    gradientFrom: "from-green-500",
    gradientTo: "to-green-600",
    animation: "animate-float-1",
    href: "/seguros-vida"
  },
  {
    title: "Seguros de Salud",
    image: "/seguro-salud.jpg",
    gradientFrom: "from-blue-500",
    gradientTo: "to-blue-600",
    animation: "animate-float-2",
    href: "/seguros-salud"
  }
];
---

<section id="planes" class=" h-[90vh] md:h-screen  flex items-center justify-center bg-gradient-to-br from-rp-gray to-white py-20">
  <div class="container mx-auto px-4">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold text-rp-blue mt-4">Nuestros Planes</h2>
      <p class="text-xl text-gray-600">Protección diseñada para cada etapa de tu vida</p>
    </div>

    <!-- Layout: Simple grid for all devices -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-16 lg:gap-20 max-w-5xl mx-auto justify-items-center">
      {plans.map((plan) => (
        <Bubble 
          title={plan.title}
          image={plan.image}
          gradientFrom={plan.gradientFrom}
          gradientTo={plan.gradientTo}
          animation={plan.animation}
          href={plan.href}
        />
      ))}
    </div>
  </div>
</section>
